/*
#include<iostream>
#include <algorithm>
using namespace std;
int score[35]; 
int main()
{
1.	int arr[]={1,5,8,9,3};
    int i;
	sort(arr,arr+5,greater<int>());
	for(i=0;i<5;i++)
	cout<<arr[i]<<" ";
	cout<<endl;
	return 0;
2.	int N, K, i;
	float average=0;
	scanf("%d",&N);
	for( i = 0 ; i < N ; i++ ){
	    scanf("%d",&score[i]);
	}
    scanf("%d",&K);
	sort(score,score+N,greater<int>());
	for( i = 0 ; i < K ; i++ ){
	    average+=score[i];
	}
	printf("%.2f",average*1.0/K);
	return 0;
}
3.
#include<iostream>
#include<algorithm>
using namespace std;
bool cmp(int x,int y) {
	return x > y;
}
int main() {
	int arr[10];
	for(int i = 0; i < 10; i++) {
		cin >> arr[i];
	}
	sort(arr, arr + 10);
	for(int i = 0; i < 10; i++) {
		cout << arr[i] << " ";
	}
	cout << endl;
	sort(arr, arr + 10, cmp);
	for(int i = 0; i < 10; i++) {
		cout << arr[i] << " ";
	}
	return 0;
}
4.
#include<iostream>
#include<algorithm>
using namespace std;
bool cmp(int x, int y) {
	if (x % 3 != y % 3) {
		return x % 3 < y % 3;
	} else {
		return x < y;
	}
}
int main() {
	int N, num[105];
	cin >> N;
	for(int i = 0; i < N; i++) {
		cin >> num[i];
	}
	sort(num, num + N, cmp);
	for(int i = 0; i < N; i++) {
		cout << num[i] << " ";
	}
	cout << endl;
	return 0;
}*/
